[package]
name = "e2e"
version = "0.0.0"
edition.workspace = true
publish = false

[lib]
# Helpers only; tests live in tests/.

[dependencies]
ctap = { path = "../ctap" }
sha2 = "0.10"
p384 = { version = "0.13", features = ["ecdsa"] }
ureq = "2"
