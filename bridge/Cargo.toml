[package]
name = "bridge"
description = "Consumer-side daemon: vsock/unix socket <-> /dev/uhid"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]

[target.'cfg(target_os = "linux")'.dependencies]
vsock = "0.5"
